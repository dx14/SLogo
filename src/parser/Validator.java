package parser;

import parser.command.CommandElement;
import parser.command.CommandTreeNode;

public class Validator {

	public static void assertArguments(CommandTreeNode tree, CommandElement rootcommand, int count) throws ParserException{
//		if(tree.getNumBranches() != count)
	}
	
}
